venv/
__pycache__/
*.pyc
*.db
*.db-journal
*.db-shm
*.db-wal
logs/
.env
docs/
knowledge_base/

# фото обходов смены — не в репозиторий
data/checklist_photos/

# macOS
.DS_Store
*.savedSearch

# локальные бэкапы/патчи, не для репозитория
*.bak
*.bak-*
*.backup
*.backup-*
*.disabled
backups/
_cleanup_archive/
ДОСТУП_ПЕРСОНАЛ.txt

# Вложения из переписки: фото шильдиков, видео узлов, акты. Это
# рабочие данные, им место в бэкапе, а не в репозитории — иначе он
# распухнет на гигабайты и утащит переписку сотрудников в открытый
# доступ вместе с кодом.
uploads/
