fastapi==0.115.0
uvicorn[standard]==0.30.6
SQLAlchemy==2.0.35
pydantic==2.9.2
python-jose[cryptography]==3.3.0
bcrypt>=4.0
python-multipart==0.0.9
# Сжатие фото в переписке и обходе смены. Главное — срезает EXIF:
# в нём с телефона приезжают GPS-координаты и модель аппарата.
Pillow>=10.0
